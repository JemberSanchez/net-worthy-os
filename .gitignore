__pycache__/
*.pyc
data/
.venv/
venv/
*.sqlite
*.db
.env
docs/guiones/PREVIEW-*.jpg
# exportaciones de render (voz + video): artefactos, no fuente — no inflar el repo
*.mp4
*.MP3
docs/guiones/*.mp3
# backups del moat (zip fechado de data/): van a nube/USB, no al repo
backups/
# ESLint se instala con `npm install --no-save eslint` solo para pasar tools/lint_motor.mjs.
# El motor sigue sin dependencias: node_modules es herramienta de desarrollo, no del producto.
node_modules/
package-lock.json
# entorno Python 3.12 aparte para Kokoro TTS (torch+spacy, ~2GB) — herramienta local, no fuente.
# El proyecto principal sigue en Python 3.14; ver tools/generar_voz.py y CLAUDE.md.
tools/.venv-voces/
